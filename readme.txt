Files in xampp/htdocs laden

Apache und Sql-Server starten

Datenbank mit Namen VierGewinnt in php MyAdmin anlegen

File user.sql importieren

Im Browser localhost/login.php aufrufen

_______________
entwicklerkommentar:

Im Browser gamelogik.php aufrufen um die funktionen für spiele zu testen.
auskommentierte funktionsaufrufe nach einander austesten

spieleroeffnen($Spieler1_ID, $Spieler2_ID, $AmZug);

auskommentierte spielid mit spielid tauschen
partiebeitreten($Spiel_ID, $userid);

setfeld($Spiel_ID, $feld, $Spieler_ID);

gebepartienaus();



//spielfeldausgabe($Spiel_ID);
